# 2026-09-23

## 提交并推送 LLM Wiki 知识库（首次入库）

- 背景：9-10 建成的知识库（19 页 wiki + `raw/` + `graph/` + `tools/` + schema 三件套）一直只在本地、从未提交，13 天零备份。
- 操作：
  - 清理 `scripts/01.ipynb`（0 字节空壳、误创建产物）；`scripts/01.py`（用户第一个 Python 脚本）入库。
  - `CLAUDE.md` / `README.md` 的素材区说明补入 `scripts/`。
  - 提交：49 文件 / +4745 行，commit `52e9dcd`。
  - 推送成功：`021014d..52e9dcd  main -> main`（88.65 KiB，耗时 12 秒）。
- 踩坑与修复：
  - **GCM 在 push 时挂起**：`git push origin main` 无输出卡死 3 分钟被超时掐断；而 `git credential fill` 正常。改用「取 PAT + 内嵌 URL + `-c credential.helper=`」后 12 秒完成。已写入项目 `MEMORY.md` 与用户级 `MEMORY.md`。
  - **本地跟踪引用怪癖复现**：push 后 `.git/refs/remotes/origin/main` 仍指向旧的 `021014d`，`git status` 假报 `ahead 1`；手动改写 loose ref 为 `52e9dcd` 后恢复。

## 追加：用户追问后发现一个漏推文件

- 用户问"是不是还有文件没推送到远程" → 严谨比对确认：**`scripts/01.ipynb`** 确实未入库（0 字节；18:11 我删过一次、18:19 检查还是干净的，18:27 又被重新创建，来源不明，疑似编辑器/Jupyter 自动生成）。
- 另一差集 `graph/graph-report.md` 是 `.gitignore` 第 14 行**主动忽略**的临时报告，属设计如此，非遗漏。
- **方法论坑（重要）**：拿 `find` 与 `git ls-files` 做 `comm` 比对时，`git ls-files` 默认把含中文的路径输出成**八进制转义**（如 `"learning-materials/\345\244\247..."`），与 `find` 的原始字节对不上，会把**已入库的中文名文件误判为"未入库"**。正确做法：git 侧加 `-c core.quotepath=false`，两侧统一 `LC_ALL=C sort` 再比对。
- **处理结果**：用户选择「保留并提交入库」→ commit `50edaee` 已推送（`8b26b46..50edaee`）。收敛后的终态：本地 **65** 文件 / 入库 **64** 文件，唯一未入库项是 `graph/graph-report.md`（`.gitignore` 主动忽略的临时报告）。

## 追加：ingest 练习 01 + 修正图谱工具不一致

- 用户说「继续」后，主动做了一件**基于现有材料、无需额外输入**的实事：把素材区里从未被 ingest 的 `exercises/ex01_描述统计入门.py` 与 `data/students_scores.csv` 吸收进知识库——这两份正好补统计簇的短板。
- **先实际运行练习取得真实数值**（数学 均值 78.65 / 中位数 81.50 / 标准差 11.82；三科均值均低于中位数 → 左偏；加 10 分后标准差不变），再据此写来源页，未编造任何数据。
- 新增 `wiki/sources/statistics-exercise-01.md`；更新 `concepts/DescriptiveStatistics.md`（补实测表、左偏判读，关闭遗留问题 2）、`index.md`、`overview.md`、`log.md`、`raw/README.md`；`raw/statistics/` 冻结两份副本（字节校验一致）。
- **发现并修掉一个真实工具不一致**：`build_graph.py` 的排除集漏了 `overview.md`（health/lint 都排除了）。后果：① 图谱节点数比体检多 1（18 vs 17）；② 更严重——两个知识簇之间那条唯一的跨簇「桥」完全由 overview 撑起，纯属假象。修正后 17 节点 / 82 边，真实情况显露：**统计簇与 AI 概念簇零连接**。
- lint 稀疏页由 2 条降为 1 条（`concepts/DescriptiveStatistics.md` 已修复）。
