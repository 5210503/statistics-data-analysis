# 2026-09-06

- 创建项目级 Skill `concept-material-generator`（.workbuddy/skills/concept-material-generator/SKILL.md）：输入任意 AI 概念名词，生成固定五部分结构的概念学习 Markdown 文档。
- 新建 `learning-materials/` 目录用于存放生成文档，并创建 `index.md` 作为查重索引（Skill 仅用 Read/Write 工具，靠索引查重而非列目录）。
- 仓库根目录 .gitignore 已存在（Python 临时文件规则），在末尾追加密钥/API Key 屏蔽规则（.env、*.pem、*.key、*apikey* 等）。
- 用该 skill 生成首份学习资料 `learning-materials/Agent-Skill.md`（概念：Agent Skill，Anthropic 2025-10-16 发布），并同步更新索引；生成前用 WebSearch/WebFetch 核实了 4 条原始出处链接（claude.com/blog/skills、Anthropic 工程博客、agentskills.io、github.com/anthropics/skills）。
- 根据用户新要求升级 skill 本体：固定五部分 → 六部分（新增"自测题"，难度递进：识别→理解→应用→辨析）；新增"呈现原则：少即是多"（文字精简、Mermaid 可视化 1-3 张、默认主题配色克制、`<details>` 折叠答案）；生成步骤新增"源头查证"。
- 生成第二份学习资料 `learning-materials/大模型上下文.md`，按升级后的 skill 六部分结构执行；源头核实了 3 条链接（arXiv:1706.03762 Transformer 论文、arXiv:2307.03172 Lost in the Middle、Anthropic context engineering 工程博客），文中与 Agent-Skill.md 做了交叉引用（渐进式披露即上下文管理策略）。
- 用户输入 "shill" 经确认为 "Skill"（Agent Skill），因索引已有同名概念，按查重规则生成 v2 深入版 `learning-materials/Agent-Skill-v2.md`（聚焦怎么写好：description 触发条件、单一工作流、评测驱动迭代）；核实了 2 条新链接（support.claude.com 创建自定义 skill 指南、github.com/anthropics/skills）。注意：docs.claude.com/platform.claude.com 文档站在当前地区不可访问，不宜作为可核查链接。
